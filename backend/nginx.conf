# Этот файл больше не используется, так как nginx вынесен в отдельный frontend контейнер
# Backend теперь работает только как FastAPI сервер на порту 8000
# 
# Если нужен nginx для backend, создайте отдельный nginx-proxy контейнер
# для роутинга между frontend и backend
